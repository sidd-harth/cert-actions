name: Debug Logging Demo

on: push

env:
  ACTIONS_STEP_DEBUG: true

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Call 
          run: curl https://httpbin.org/ip