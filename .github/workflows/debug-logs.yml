name: Debug Logging Demo

on: push

env:
  ACTIONS_RUNNER_DEBUG: true

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Call 
          run: curl -X GET https://httpbin.org/bearer